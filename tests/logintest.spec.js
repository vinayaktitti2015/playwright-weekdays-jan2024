import {test, expect} from '@playwright/test'

// test suite - no of test cases
test.describe('login feature tests', function () {

    // hooks or annotations
    test.beforeEach(async ({page}) =>{
        
    })
});
